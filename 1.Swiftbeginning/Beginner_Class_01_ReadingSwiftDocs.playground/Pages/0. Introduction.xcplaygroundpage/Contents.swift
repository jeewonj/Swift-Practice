/*:
 ![메인이미지](image_01.png)
 
 # SwiftUI 공식문서 읽는 법
 ### Text로 배우는 공식문서 해독법

 * Callout(학습 목표):
   - 함수 시그니처를 읽을 수 있다.
   - `init`이 무엇인지 이해할 수 있다.
   - Instance Method와 체이닝의 원리를 이해할 수 있다.
   - 파라미터 타입을 보고 올바른 값을 넣을 수 있다.

 ## 왜 공식문서가 어려울까?

 SwiftUI를 처음 배우고 공식문서를 열면 이런 것들이 보여요.  

 ```swift
 func strikethrough(Bool, color: Color?) -> Text
 func font(Font?) -> Text
 init(verbatim: String)
 ```

 지금은 무슨 말인지 몰라도 괜찮아요.  
 이 Playground를 마치면 저 표기법이 자연스럽게 읽혀요.  
 

 ## 우리가 배울 것들

 - **Step 1** : 함수란 무엇인가
 - **Step 2** : 함수 시그니처 읽는 법
 - **Step 3** : `init` — Text를 만드는 함수
 - **Step 4** : 여러 개의 `init`
 - **Step 5** : Instance Method — 점(.)의 의미
 - **Step 6** : Method Chaining — 왜 이어 붙일 수 있나
 - **Step 7** : 타입 읽기 — 파라미터에 뭘 넣을까
 - **Step 8** : Optional — `?`가 붙으면 뭐가 다를까
 - **Step 9** : 마무리 — 공식문서 한 줄 직접 해석하기
 */
    
//: [Next](@next)
